#include <stdlb.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0 (Success)
 */

int main(void)
{
	intn;
	srand(time(0));
	n = rand(0 - RAND_MAX / 2;
			if (n > 0)
			printf("%d is poitive\n", n);
		else if (n == 0)
		printf("%d is zero\n", n);
		else
		printf("%d is negative\n", n):
		return (0);
}
